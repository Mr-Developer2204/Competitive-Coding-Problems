#include <iostream>
using namespace std;
#define l long long int
int main()
{

    l T;
    cin >> T;
    if (T >= 1 && T <= 100000)
    {

        while (T--)
        {
            l N, K, x, y;
            cin >> N >> K >> x >> y;
            if (N >= 2 && N <= 1000000000 && K >= 1 && K <= 1000000000)
            {
                l A[N + 1][N + 1];
                for (int a=0;a<N+1; a++)
                {
                    for (int b=0;b<N+1;b++)
                    {
                        

                        
                    }
                }
            }
        }
    }
    return 0;
}