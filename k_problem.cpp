#include <iostream>
#include<string.h>
using namespace std; 

int main()
{
      int t, n, k;
      cin >> t; 
      for (int i = 1; i <= t; ++i) {
      cin >> N >> K; 
      char ch;
        string s;
        for(int j=0;j<N;j++)
        {
            cin>>ch;
            s =s +ch;
        }
        int n = N;
        for(int j = 0;j<((N/2)-1);j++)
        {
           if(s[j]==) 
        }
        
        
        cout << "Case #" << i << ": " << << " " << << endl;
        
        
      }
      return 0;
    }